class User {
    constructor(id, email, password, rides, dni) {
      (this.id = id),
        (this.email = email),
        (this.password = password),
        (this.rides = rides),
        (this.dni = dni)
    }
  }
  
  export default User;