class Station {
    constructor(id, address, image, lat, longi) {
      (this.id = id),
      (this.address = address),
      (this.image = image),
        (this.lat = lat),
        (this.longi = longi)
    }
  }
  
  export default Station;